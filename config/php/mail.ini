; Development mail safety.
;
; PHP's built-in mail() has no transport in this container on purpose:
; nothing should ever be able to reach a real inbox from development.
; WordPress mail is routed to Mailpit over SMTP by the YKA Core
; development module (inc/class-dev-mail.php), which hooks phpmailer_init.
sendmail_path = /bin/false
