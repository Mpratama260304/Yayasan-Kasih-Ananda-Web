; PHP overrides for the YKA development container.
; Editorial teams upload camera photographs, so the defaults are too small.
upload_max_filesize = 128M
post_max_size = 128M
max_execution_time = 300
max_input_time = 300
memory_limit = 512M
max_input_vars = 5000
